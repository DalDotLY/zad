# قواعد مشروع زاد القوافل

- الموقع عربي أولًا، واتجاهه RTL في جميع الصفحات.
- استخدم بيانات الشركة المركزية من `lib/site-config.ts`.
- لا تضف أرقامًا أو عناوين أو فروعًا أو شهادات أو إحصاءات غير موثقة.
- حافظ على ألوان الهوية: `#18232D` و`#7A1F2B` و`#D8C3A5` و`#F6F1E8`.
- استخدم ملفات الشعار الأصلية داخل `public`.
- حافظ على الوصول، التباين، حالات التركيز، و`prefers-reduced-motion`.
- اختبر `pnpm lint` و`pnpm exec tsc --noEmit` و`pnpm build` قبل التسليم.
